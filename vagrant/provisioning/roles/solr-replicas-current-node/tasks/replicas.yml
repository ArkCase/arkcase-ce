- name: create replicas on current node
  uri:
    url: https://{{ solr_host }}:8983/solr/admin/collections?action=ADDREPLICA&collection={{ item }}&node={{ solr_host }}:8983_solr&shard=shard1
    timeout: 3600
  when: solr_host | default("") != ""

