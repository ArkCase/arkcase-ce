- name: list solr_cores
  debug:
    var: solr_cores

- name: add {{ external_host }} to hosts file
  become: yes
  lineinfile:
    path: /etc/hosts
    line: "{{ item }}"
  loop:
    - "{{ external_host_entry | default('') }}"
