arkcase:
  kafka:
    bootstrapAddress: {{ kafka_host }}:{{ kafka_port | default(9092) }}
    schemaRegistryAddress: https://{{ internal_host }}/schema-registry
  eureka:
    defaultZone: https://{{ internal_host }}/eureka/
  zipkin:
    baseUrl: https://{{ zipkin_host | default(internal_host) }}/zipkin/api
